# Models

This folder contains trained neural posterior-estimation models and their
checkpoints, organized by experiment:

- `wimpy/`: models for the WimPyDD experiments
- `xenon/`: models for the XENON experiments

Training scripts are in `sbi_training/`; model architectures are defined in
`utils/architectures.py`.
